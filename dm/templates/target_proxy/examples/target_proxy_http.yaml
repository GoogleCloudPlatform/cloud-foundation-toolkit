# Example of the target proxy template usage.
#
# Replace the <FIXME:urlMap> placeholder with a valid
# URL of the UrlMap resource.

imports:
  - path: templates/target_proxy/target_proxy.py
    name: target_proxy.py

resources:
  - name: target-http-proxy
    type: target_proxy.py
    properties:
      protocol: HTTP
      target: <FIXME:urlMap>
