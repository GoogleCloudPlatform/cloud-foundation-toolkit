name: test-compute-firewall
description: Firewall types test

imports:
  - path: ../../../../dm/templates/firewall/firewall.py
    name: firewall.py

resources:
  - type: firewall.py
    name: test-compute-firewall-prod
    properties:
      network: $(out.test-network.test-network-prod-1.name)
      rules:
        - name: allow-proxy-from-inside-prod
          allowed:
            - IPProtocol: tcp
              ports:
                - "80"
                - "444"
          description: This rule allows connectivity to the HTTP proxies
          direction: INGRESS
          sourceRanges:
            - 10.0.0.0/8
        - name: allow-dns-from-inside-prod
          allowed:
            - IPProtocol: udp
              ports:
                - "53"
            - IPProtocol: tcp
              ports:
                - "53"
          description: this rule allows DNS queries to google's 8.8.8.8
          direction: EGRESS
          destinationRanges:
            - 8.8.8.8/32
  - type: firewall.py
    name: test-compute-firewall-dev
    properties:
      network: $(out.test-network.test-network-dev-1.name)
      rules:
        - name: allow-proxy-from-inside-dev
          allowed:
            - IPProtocol: tcp
              ports:
                - "80"
                - "444"
          description: This rule allows connectivity to the HTTP proxies
          direction: INGRESS
          sourceRanges:
            - 10.0.0.0/8
